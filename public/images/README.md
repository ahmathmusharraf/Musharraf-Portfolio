# Upload your custom images here
You can place your image files (e.g. `profile.jpg`, `project-1.jpg`, `logo.png`) inside this directory (`/public/images/`).

In your code or constants, you can reference them using `/images/your-image-name.jpg`.
